class Solution:
    def diameterOfBinaryTree(self, root: Optional[TreeNode]) -> int:
        # if root == None:
        #     return 0
        
        # hlst = 0
        # hrst = 0
        # def height(root):
        #     if root == None:
        #         return 0
            
        #     lf = height(root.left)
        #     rf = height(root.right)
            
        #     return max(lf, rf) + 1
        
        # hlst = height(root.left)
        # hrst = height(root.right)
        
        # return hlst + hrst

        # this is not working for the case of excluding root node

        

